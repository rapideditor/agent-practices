# Please see the documentation for all configuration options:
# https://help.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "monthly"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"

# sync:
# version=1
# source=https://github.com/rapideditor/agent-practices/blob/main/templates/.github/dependabot.yml
# instructions="skip entirely if this project already has Dependabot configured; otherwise adapt the package-ecosystem entries to match this project's actual dependency managers"
